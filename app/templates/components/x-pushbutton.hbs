{{#if state}}
  <button class="uk-button uk-button-small active {{extraClass}} " {{action "toggle"}}>
    {{yield}}
  </button>
{{else}}
  <button class="uk-button uk-button-small {{extraClass}}" {{action "toggle"}}>
    {{yield}}
  </button>
{{/if}}
