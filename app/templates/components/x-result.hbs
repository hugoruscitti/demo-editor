{{#if project}}

<h3>Título {{project.title}}</h3>

  <div class="button-container">

  <button type="button" {{action "run" project}} class="uk-button uk-button-success">Ejecutar</button>
</div>

<div class="canvas-container">
  {{x-canvas tagName=""}}
</div>


{{#if errors}}

  {{#each errors as |error_message|}}
    <div class="uk-alert uk-alert-danger">
      {{error_message}}
    </div>
  {{/each}}

{{/if}}


{{yield}}

{{else}}
Se necesita un proyecto.
{{/if}}
