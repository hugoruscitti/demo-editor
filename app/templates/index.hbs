{{#x-header}}

  <div class="pull-left">
    <div class="uk-button-group">
      {{#link-to "index" tagName="button" class="uk-button uk-button-mini"}} {{fa-icon "home"}} {{/link-to}}
      {{#link-to "project" tagName="button" class="uk-button uk-button-mini"}} {{fa-icon "briefcase"}} {{/link-to}}
    </div>
  </div>

  <div class="pull-right">
    {{#link-to "index.about" tagName="button" class="uk-button uk-button-mini"}}Acerca de ...{{/link-to}}
  </div>

{{/x-header}}



<div class="window-content">
  <div class="pane">

    <div class="padding text-center">

      <div class="img pilas_logo_128"></div>

      <p>¡Hola, te damos a bienvenida a pilas-editor!</p>

      <div>
        {{x-mainButton icon="add_folder" text="Proyectos" route="project"}}
        {{x-mainButton icon="user_manual" text="Ver el manual" route="manual"}}
      </div>

      <div>
        {{x-mainButton icon="settings" text="Configuración" route="index.setup"}}
        {{x-mainButton icon="filled_box" text="Ejemplos" route="examples"}}
      </div>

      {{outlet}}

      <div class='text-center padding'>
        Versión: {{app-version}}
      </div>

    </div>

  </div>
</div>
