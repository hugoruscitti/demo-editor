{{#modal-dialog close="toggleModal" focusSelector="a#close" container-class="setup-dialog"}}
<div class="dialog-container">

  <p class="dialog-title">Configuración</p>

  <label for="vim">
    {{input id="vim" type="checkbox" checked=preferences.vimMode}}

    Modo VIM en el editor (no recomendado para el 90% de los usuarios)
  </label>

  <p>
    Tema: <button {{action "changeTheme"}} class="uk-button">{{preferences.theme}}</button>
  </p>

  <div class="dialog-buttons">
    {{#link-to "index" id="close" tagName="button" class="uk-button"}}Cerrar{{/link-to}}
  </div>

</div>
{{/modal-dialog}}
